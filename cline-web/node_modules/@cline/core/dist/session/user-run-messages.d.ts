export type MessageDisplayRole = "user" | "assistant" | "tool" | "system" | "status" | "error";
type MessageLike = {
    role?: unknown;
    content?: unknown;
    metadata?: unknown;
};
/** Resolves the role a persisted message should use when presented to a user. */
export declare function resolveMessageDisplayRole(message: MessageLike): MessageDisplayRole;
/**
 * Returns how many absolute root runs a persisted message represents.
 *
 * Compaction can fold several earlier user turns into one synthetic message.
 * Its persisted span keeps later visible prompts aligned with checkpoint
 * history. Tool results and system-injected user-role messages contribute no
 * runs.
 */
export declare function getUserRunSpan(message: MessageLike): number;
export declare function isUserRunMessage(message: MessageLike): boolean;
export declare function countUserRunMessages(messages: readonly MessageLike[] | undefined): number;
export {};
