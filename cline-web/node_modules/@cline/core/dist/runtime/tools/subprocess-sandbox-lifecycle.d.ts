export declare const MAX_NODE_TIMER_DELAY_MS = 2147483647;
type ParentProcess = {
    once(event: "disconnect", listener: () => void): unknown;
    exit(code: number): unknown;
};
export declare function normalizeIdleTimeoutMs(timeoutMs: number | undefined): number | undefined;
/** Ensure an IPC child cannot survive the process that owns it. */
export declare function installParentDisconnectGuard(target?: ParentProcess): void;
export {};
