export type ProcessStartTokenProbeResult = {
    status: "found";
    token: string;
} | {
    status: "missing";
} | {
    status: "unavailable";
};
export declare function parseLinuxProcessStartToken(stat: string, bootId: string): string | undefined;
/** Distinguishes an absent process from an unavailable identity probe. */
export declare function probeProcessStartToken(pid: number): ProcessStartTokenProbeResult;
/**
 * Returns a token for one generation of a process ID.
 *
 * A PID alone is not an identity because the operating system can reuse it
 * after a process exits. The token is derived from the kernel-reported process
 * start time so callers can distinguish a live process from a later process
 * that inherited the same PID.
 */
export declare function getProcessStartToken(pid: number): string | undefined;
/** Non-blocking probe for latency-sensitive runtime paths. */
export declare function probeProcessStartTokenAsync(pid: number): Promise<ProcessStartTokenProbeResult>;
export declare function getProcessStartTokenAsync(pid: number): Promise<string | undefined>;
