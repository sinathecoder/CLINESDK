"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var index_exports = {};
__export(index_exports, {
  LangfuseVercelAiSdkIntegration: () => LangfuseVercelAiSdkIntegration
});
module.exports = __toCommonJS(index_exports);

// src/LangfuseVercelAiSdkIntegration.ts
var import_otel = require("@ai-sdk/otel");

// src/utils.ts
var import_core = require("@langfuse/core");
var PROMPT_SPAN_TYPES = /* @__PURE__ */ new Set([
  "languageModel",
  "embedding",
  "reranking"
]);
function createLangfuseObservationAttributes(params) {
  const { runtimeContext, spanType } = params;
  const attributes = {};
  if (!runtimeContext) {
    return attributes;
  }
  const { langfusePrompt, ...metadata } = runtimeContext;
  const prompt = normalizePrompt(langfusePrompt);
  if (PROMPT_SPAN_TYPES.has(spanType) && prompt && !prompt.isFallback) {
    attributes[import_core.LangfuseOtelSpanAttributes.OBSERVATION_PROMPT_NAME] = prompt.name;
    attributes[import_core.LangfuseOtelSpanAttributes.OBSERVATION_PROMPT_VERSION] = prompt.version;
  }
  if (metadata) {
    for (const [key, value] of Object.entries(metadata)) {
      const serialized = typeof value === "string" ? value : safeSerialize(value);
      if (serialized != null) {
        attributes[`${import_core.LangfuseOtelSpanAttributes.OBSERVATION_METADATA}.${key}`] = serialized;
      }
    }
  }
  return attributes;
}
function normalizePrompt(value) {
  if (!isPlainObject(value)) {
    return;
  }
  if (typeof value.name !== "string" || typeof value.version !== "number") {
    return;
  }
  return {
    name: value.name,
    version: value.version,
    isFallback: typeof value.isFallback === "boolean" ? value.isFallback : false
  };
}
function isPlainObject(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function safeSerialize(value) {
  try {
    if (value == null) {
      return;
    }
    return JSON.stringify(value);
  } catch {
    return "<failed to serialize>";
  }
}

// src/LangfuseVercelAiSdkIntegration.ts
var LangfuseVercelAiSdkIntegration = class {
  constructor(options = {}) {
    const openTelemetryOptions = {
      tracer: options.tracer,
      enrichSpan: ({ spanType, runtimeContext }) => createLangfuseObservationAttributes({
        spanType,
        runtimeContext
      })
    };
    this.delegate = new import_otel.OpenTelemetry(openTelemetryOptions);
  }
  executeTool(params) {
    return this.delegate.executeTool(params);
  }
  executeLanguageModelCall(params) {
    return this.delegate.executeLanguageModelCall(params);
  }
  onStart(event) {
    this.delegate.onStart(event);
  }
  onStepStart(event) {
    this.delegate.onStepStart(event);
  }
  onLanguageModelCallStart(event) {
    this.delegate.onLanguageModelCallStart(event);
  }
  onLanguageModelCallEnd(event) {
    this.delegate.onLanguageModelCallEnd(event);
  }
  onToolExecutionStart(event) {
    this.delegate.onToolExecutionStart(event);
  }
  onToolExecutionEnd(event) {
    this.delegate.onToolExecutionEnd(event);
  }
  onStepEnd(event) {
    this.delegate.onStepEnd(event);
  }
  /** @deprecated AI SDK v7 still emits object generation model spans through this callback. */
  onObjectStepStart(event) {
    this.delegate.onObjectStepStart(event);
  }
  /** @deprecated AI SDK v7 still emits object generation model spans through this callback. */
  onObjectStepEnd(event) {
    this.delegate.onObjectStepEnd(event);
  }
  onEmbedStart(event) {
    this.delegate.onEmbedStart(event);
  }
  onEmbedEnd(event) {
    this.delegate.onEmbedEnd(event);
  }
  onRerankStart(event) {
    this.delegate.onRerankStart(event);
  }
  onRerankEnd(event) {
    this.delegate.onRerankEnd(event);
  }
  onEnd(event) {
    this.delegate.onEnd(event);
  }
  onAbort(event) {
    this.delegate.onAbort(event);
  }
  onError(error) {
    this.delegate.onError(error);
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  LangfuseVercelAiSdkIntegration
});
//# sourceMappingURL=index.cjs.map